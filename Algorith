Algorithme PGCD_EUCLIDE
Variables
   A, B, r : entier
Début
   Ecrire("Entrez la valeur du premier nombre A : ")
   Lire(A)
   Ecrire("Entrez la valeur du deuxième nombre B : ")
   Lire(B)
   TantQue (B <> 0) faire
        r ← A mod B
        Ecrire(A, " = ", B, " × ", (A div B), " + ", r)
        A ← B
        B ← r
   FinTantQue
   Ecrire("Le PGCD est : ", A)
Fin
